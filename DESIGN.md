# PDF編集アプリ 設計書（Design Doc）

## 1. 概要

ブラウザ上で動作する、ホビーユース向けのPDF編集Webアプリ。iLovePDF のような「機能ごとのツール集」型のUXを目指す。まずは **マージ・分割・回転** の3機能をMVPとして実装し、将来的に圧縮・プレビュー等へ拡張する。

### 設計の核となる方針

完全クライアントサイドで処理を完結させる。PDFファイルはサーバーにアップロードせず、ユーザーのブラウザ内（メモリ上）でのみ処理する。これによりプライバシーが保たれ、サーバー運用コストもゼロになる。

---

## 2. 目的とゴール

| 項目 | 内容 |
|---|---|
| 主目的 | 既存PDFのマージ・分割・回転を手軽に行えるWebアプリ |
| 対象ユーザー | 個人ユーザー（ホビー用途）。たまにPDFを整理したい人 |
| 非ゴール | 商用SaaS化、OCR、AI要約、リアルタイム共同編集（少なくとも初期は対象外） |
| 成功基準 | 3機能がブラウザ内で完結して動作し、ファイルがサーバーに送信されないこと |

---

## 3. 技術スタック

| レイヤー | 採用技術 | 理由 |
|---|---|---|
| フレームワーク | React + TypeScript | 型安全。pdf-lib がTS製で相性が良い |
| PDF処理エンジン | pdf-lib | マージ・分割・回転をブラウザ内で完結。依存なし・MITライセンス |
| プレビュー（将来） | PDF.js | サムネイル・ページ表示用 |
| 圧縮（将来） | Ghostscript WASM | 本格的な圧縮が必要になった場合のみ |
| スタイリング | （自前で決定） | フロントは自作前提 |

### ライブラリ役割分担

pdf-lib が処理の中核を担う。マージ・分割・回転はすべてこのライブラリだけで実現できる。プレビュー表示や本格圧縮など pdf-lib が苦手な領域だけ、後から PDF.js や Ghostscript WASM を追加する段階的な構成とする。

---

## 4. 機能要件

### 4.1 マージ（結合）

複数のPDFファイルを1つのファイルに結合する。

- 複数ファイルのアップロードに対応
- ファイルの順序をユーザーが並び替えできる（ドラッグ&ドロップ想定）
- 結合後のファイルをダウンロード

実装の中核は pdf-lib の `copyPages()` で各ドキュメントからページをコピーし、`addPage()` で新規ドキュメントへ追加する流れ。配列の順序がそのまま結合順になるため、UI側の並び順を保持した配列を渡すだけでよい。

### 4.2 分割

1つのPDFを複数のファイルに分割する。

- ページ範囲指定（例: 1-3ページ目、4-6ページ目）
- 1ページずつ全分割するモード
- 分割後の複数ファイルをまとめてダウンロード（必要ならZIP化を検討）

範囲指定UIから受け取ったページ範囲配列をもとに、範囲ごとに新規ドキュメントを作成してページをコピーする。

### 4.3 回転

PDFの指定ページ、または全ページを回転する。

- 90度 / 180度 / 270度の回転
- 全ページ一括 / 個別ページ指定
- 回転後のファイルをダウンロード

`getRotation().angle` で現在の回転角を取得し、加算した値を `setRotation()` で設定する。既存の回転状態を考慮した加算方式とする。

---

## 5. 非機能要件

| 項目 | 要件 |
|---|---|
| プライバシー | ファイルをサーバーへ送信しない（クライアント完結） |
| パフォーマンス | 大きなPDFでもUIが固まらないこと（重い処理はWeb Workerを検討） |
| 対応ブラウザ | モダンブラウザ（Chrome / Firefox / Safari / Edge の最新版） |
| エラー処理 | 破損PDF・非対応ファイルを読み込んだ際に分かりやすく通知 |
| メモリ | 端末のRAMに依存。100MB程度までは現実的に処理可能 |

---

## 6. アーキテクチャ

### 6.1 全体構成

```
┌─────────────────────────────────────────┐
│              ブラウザ                       │
│                                          │
│  ┌────────────┐      ┌────────────────┐  │
│  │   UI層      │ ───→ │  PDF処理層       │  │
│  │ (React/TSX) │      │  (pdf-lib)      │  │
│  └────────────┘      └────────────────┘  │
│        ↓                     ↓            │
│  ┌────────────┐      ┌────────────────┐  │
│  │ ファイル入力 │      │ ダウンロード処理  │  │
│  │ (File API)  │      │ (Blob/URL)     │  │
│  └────────────┘      └────────────────┘  │
└─────────────────────────────────────────┘
        ※ サーバーへの通信は一切なし
```

### 6.2 レイヤー分離

UI層とPDF処理層を明確に分離する。処理層は「ArrayBuffer を受け取り Uint8Array を返す純粋関数」として実装し、UIから独立してテスト可能にする。これにより機能追加時に処理ロジックだけを差し替えやすくなる。

### 6.3 ディレクトリ構成（案）

```
src/
├── components/        # UIコンポーネント
│   ├── FileUploader/
│   ├── PageList/      # 並び替え・選択UI
│   └── ToolPanel/
├── lib/               # PDF処理（UIから独立）
│   ├── merge.ts
│   ├── split.ts
│   ├── rotate.ts
│   └── download.ts
├── hooks/             # Reactフック
├── types/             # 型定義
└── App.tsx
```

---

## 7. データフロー

すべての機能で共通の流れをとる。

1. ユーザーがファイルを選択（File API）
2. `file.arrayBuffer()` で ArrayBuffer に変換
3. pdf-lib の処理関数に渡す（merge / split / rotate）
4. 処理結果の Uint8Array を受け取る
5. Blob 化して `URL.createObjectURL` でダウンロードリンクを生成
6. ダウンロード後に `URL.revokeObjectURL` でメモリ解放

処理層のインターフェースは統一する。入力はファイル由来の ArrayBuffer、出力は Uint8Array（分割のみ配列）とすることで、UI側の扱いがシンプルになる。

---

## 8. UI設計の方針

iLovePDF型の「トップに機能カードが並び、選ぶと専用画面に遷移する」構造を踏襲する。

- トップ画面: マージ / 分割 / 回転 のカードを表示
- 各ツール画面: ファイル投入 → 設定 → 実行 → ダウンロード の一直線フロー
- 状態は「アップロード前 / 設定中 / 処理中 / 完了」の4ステートで管理

処理中はスピナーを表示し、完了後はダウンロードボタンと「別のファイルを処理」への導線を出す。エラー時は原因（破損・非対応形式など）を文言で示す。

---

## 9. 段階的な開発計画

| フェーズ | 内容 | 使用ライブラリ |
|---|---|---|
| Phase 1 | マージ機能（最もシンプル） | pdf-lib |
| Phase 2 | 分割機能 | pdf-lib |
| Phase 3 | 回転機能 | pdf-lib |
| Phase 4 | ページサムネイルのプレビュー | + PDF.js |
| Phase 5 | 圧縮機能（必要なら） | + Ghostscript WASM |

まず Phase 1〜3 を pdf-lib だけで完成させ、動くものを最短で作る。プレビューや圧縮は「あったら便利」な拡張として後回しにする。

---

## 10. リスクと検討事項

| リスク / 課題 | 対応方針 |
|---|---|
| 大きなPDFでUIが固まる | 重い処理を Web Worker に逃がす |
| 分割で多数ファイルが出る | ZIPにまとめてダウンロード（jszip等を検討） |
| 圧縮のライセンス | Ghostscript WASM は AGPL。ホビー用途なら可だが商用展開時は要注意 |
| 暗号化PDF | パスワード付きPDFは pdf-lib で読めない場合がある。エラー通知で対応 |
| ブラウザのメモリ上限 | 超大容量ファイルは非対応と割り切る／警告表示 |

---

## 11. 将来の拡張候補

初期スコープ外だが、需要に応じて検討する機能。

- ページの削除・並び替え（pdf-lib で対応可能）
- 画像→PDF変換 / PDF→画像変換
- 透かし・ページ番号の追加（pdf-lib で対応可能）
- パスワード保護の付与・解除
- フォーム入力

これらの多くは pdf-lib の範囲で実装できるため、コア構成を変えずに追加していける。

---

## 付録: 主要ライブラリ一覧

| ライブラリ | 用途 | ライセンス |
|---|---|---|
| pdf-lib | マージ・分割・回転・テキスト/画像追加・フォーム | MIT |
| PDF.js | 表示・サムネイル（編集不可） | Apache 2.0 |
| Ghostscript WASM | 本格的な圧縮 | AGPL |
| jszip | 分割ファイルのZIPまとめ（候補） | MIT/GPL |